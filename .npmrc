# Security hardening
# Refuse packages published less than 3 days ago (blocks zero-day supply chain attacks)
min-release-age=3

# Disable postinstall scripts (prevents malicious scripts from running on install)
ignore-scripts=true

# Pin exact versions (no ^ auto-upgrades)
save-exact=true
